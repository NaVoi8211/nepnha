# --- Gradle / build ---
.gradle/
/build
app/build/
/captures
.cxx
.externalNativeBuild

# --- Kotlin compiler session data (Kotlin 2.x) ---
.kotlin/

# --- Máy cục bộ: chứa đường dẫn SDK, KHÔNG commit ---
local.properties

# --- Bí mật ký bản phát hành: TUYỆT ĐỐI KHÔNG commit ---
# `keystore.properties` chứa mật khẩu; `*.jks`/`*.keystore` là chính khoá ký.
# Lộ upload key là mất quyền cập nhật app trên Play.
keystore.properties
*.jks
*.keystore

# --- IDE ---
.idea/
*.iml

# --- macOS ---
.DS_Store

# LƯU Ý: app/schemas/ (Room schema export) CỐ Ý được commit
# để review diff khi làm migration.
